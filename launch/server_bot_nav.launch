<launch>
  <!-- Arguments -->
  <arg name="map_file" default="$(find server_bot)/maps/my_map.yaml"/>
  <arg name="open_rviz" default="true"/>
  <arg name="move_forward_only" default="false"/>
  
  <!-- map to odom will be provided by the gmapping -->
  <!-- <node pkg="tf" type="static_transform_publisher" name="map_to_odom" args="0 0 0 0 0 0 map odom 10" /> -->

  <!-- Server Bot -->
  <include file="$(find server_bot)/launch/gazebo2.launch">
  </include>

  <!-- Map server -->
  <node pkg="map_server" name="map_server" type="map_server" args="$(arg map_file)"/>

  <!-- AMCL -->
  <include file="$(find server_bot)/launch/amcl.launch"/>

  <!-- move_base -->
  <include file="$(find server_bot)/launch/move_base.launch">
    <arg name="move_forward_only" value="$(arg move_forward_only)"/>
  </include>

  <!-- rviz -->
  <group if="$(arg open_rviz)"> 
    <node pkg="rviz" type="rviz" name="rviz" args="-d $(find server_bot)/rviz/map_bot.rviz"/>
  </group>  
</launch>